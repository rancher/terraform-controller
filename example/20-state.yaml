apiVersion: terraformcontroller.cattle.io/v1
kind: State
metadata:
  name: my-state
spec:
  moduleName: my-module
  destroyOnDelete: true
  autoConfirm: true
  variables:
    secretNames:
      - mysecret
    envConfigNames:
      - env-config
